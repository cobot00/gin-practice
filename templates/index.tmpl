<html>
  <link rel="stylesheet" href="css/application.css">

  <h1>
    {{ .title }}
  </h1>

  <ul>
    {{range .users}}
    <li class="flex-container">
      <div style="width: 150px">
        <b>Name:</b> {{.Name}}
      </div>
      <div style="width: 150px">
        <b>Age:</b> {{.Age}}
      </div>
      <div style="width: 150px">
        <b>Note:</b> {{.Note}}
      </div>
    </li>
    {{end}}
  </ul>
</html>
